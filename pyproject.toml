[project]
name = "green-smart"
version = "1.8.2"
description = "Home Assistant custom integration for Green Smart greenhouse management"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = [
  "pytest>=8.0",
]

[tool.ruff]
line-length = 100
