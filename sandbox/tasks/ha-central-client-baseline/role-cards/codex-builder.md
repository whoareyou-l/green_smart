# Codex builder role card

Mission: implement the HA central client/store/config-flow baseline via TDD and run the local product test loop.

Allowed workspace:
- `/home/smartfarm/green_smart` source/tests/docs for this task only

Forbidden:
- self-approve spec/security review
- edit independent review verdicts as if they were external approvals
- touch secrets, customer data, HA runtime `.storage`, production deploy config, Docker socket, Cloudflare, MQTT, or DB runtime

Expected output:
- changed files list
- RED/GREEN test evidence
- known limitations
