# Codex builder role card

Mission: implement the HA central activation wizard UI via TDD.

Allowed:
- edit `/home/smartfarm/green_smart` panel/tests/docs/task artifacts for this task

Forbidden:
- self-approve independent review
- store activation codes/tokens in localStorage/config entry/frontend
- expose `/vendor/proxy` or arbitrary vendor path/method/feature inputs
